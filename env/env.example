# Example env file for Mobile Developer
# Copy to .env and adjust as needed

# code-server port
PORT=8888

# Paths for HTTPS certs (if you want to override defaults)
CODE_SERVER_CERT=$HOME/code-server.crt
CODE_SERVER_KEY=$HOME/code-server.key

# Tailscale auth key (optional, for auto-login in headless/scripted)
# TAILSCALE_AUTHKEY=

# code-server password (optional, otherwise uses default config)
# CODE_SERVER_PASSWORD=
